[build-system]
requires = [
    "setuptools >= 46.4.0",
    "versioningit ~= 2.3",
    "wheel ~= 0.32"
]
build-backend = "setuptools.build_meta"

[tool.versioningit.write]
file = "heudiconv/_version.py"
