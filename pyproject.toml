[build-system]
requires = [
    "setuptools >= 46.4.0",
    "versioningit ~= 1.0",
    "wheel ~= 0.32"
]
build-backend = "setuptools.build_meta"

[tool.versioningit.write]
file = "heudiconv/_version.py"
